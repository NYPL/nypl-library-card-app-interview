node_modules
dist
bower_components
docs
coverage
.DS_Store
npm-debug.log
.bower
.vscode
.eslintcache

# WebStorm IDE
.idea
# Elastic Beanstalk Files
.elasticbeanstalk/*
!.elasticbeanstalk/*.cfg.yml
!.elasticbeanstalk/*.global.yml

.next
.env*.local
# Committed on purpose in this copy: it holds no credentials, only the mock
# API URLs, so the app runs as soon as the repo is cloned.
!.env.local
.env.prod
.env.buid
npm-debug.log*
yarn-debug.log*
yarn-error.log*

certificates

*.log

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
